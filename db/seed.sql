-- Seed data (templates are uploaded via the API, not seeded here)
-- This file is intentionally minimal.

-- Insert a placeholder so the file is valid
DO $$ BEGIN
  RAISE NOTICE 'BIDV Agent DB initialized successfully';
END $$;
